 
var ship

function preload(){
seaImg = loadImage("sea.png");
shipImg1=loadAnimation(//list the naes the file that you want to load)
}

function setup(){
  createCanvas(400,400);
 spriteName.addImg(seaImg); 
}

function draw() {
  background("blue");
 //code to reset the background
 if (sea.x<0){
sea.x=sea.width/2;


 }
}